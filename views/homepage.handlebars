<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workout Wizards</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
</head>
<body>

    <nav class="navbar is-dark" role="navigation" aria-label="main navigation"> <!-- You can change this color class -->
        <div class="navbar-brand">
            <h1 class="navbar-item title is-3">Workout Wizards</h1>
        </div>

        <div class="navbar-menu">
            <div class="navbar-start">
                <a href="/home" class="navbar-item">Home</a>
                <a href="/calendar" class="navbar-item">Calendar</a>
                <a href="/exercise" class="navbar-item">Exercise</a>
                <a href="/credentials" class="navbar-item">Credentials</a>
            </div>
        </div>
    </nav>

    <section class="hero is-medium is-primary is-bold">
        <div class="hero-body">
            <div class="container">
                <p class="title">A Web Application Where You Can Track Your Everyday Workout Routines.</p>
            </div>
        </div>
    </section>

    <main class="section">
        <div class="container">

            {{#if logged_in}}
                <p>Welcome back! Check out your latest workout routines.</p>
            {{else}}
                <p>Please log in to track your workouts.</p>
                <a href="/login" class="button is-primary">Login</a>
            {{/if}}

            <section class="posts">

                {{#if posts.length}}
                    {{#each posts}}
                    <article class="box">
                        <h2 class="title is-4">{{this.title}}</h2>
                        <p>{{this.content}}</p>
                        <a href="/post/{{this.id}}" class="button is-link">Read more</a>
                    </article>
                    {{/each}}
                {{else}}
                    <p class="has-text-centered">No posts available at the moment. Please check back later.</p>
                {{/if}}
            </section>

        </div>
    </main>

    <footer class="footer">
        <div class="content has-text-centered">
            <p>&copy; 2024 Workout Wizards. All rights reserved.</p>
        </div>
    </footer>

</body>
</html>
